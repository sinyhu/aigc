# git 的命令记录

git branch -a  查询有哪些分支
git checkout AIBrush orign/AIBrush

git 服务器上执行下面两个命令
git init --bare androidAIBrush
chown -R git:git androidAIBrush

本地获取然后创建远程分支
git pull origin master

git add 1.txt 

git commit -m "提交1.txt"

git push origin master
